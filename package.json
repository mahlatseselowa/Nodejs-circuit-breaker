{
  "name": "circuit-breaker",
  "version": "1.0.0",
  "description": "Fault -tolerance and circuit break resilence servers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start-server": "npm run build && node build/index.js",
    "test-breaker": "npm run build && node build/test.js"
  },
  "keywords": [
    "fault-tolerance",
    "resilent",
    "circuit-breakers"
  ],
  "author": "Mahlatse Selowa",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "axios": "^0.26.0"
  }
}
